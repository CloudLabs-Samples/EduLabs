## MetaData
Question Type : Text Input

## Question
6. The report job on appvm requested its managed identity token from the Azure Instance Metadata Service (IMDS). This endpoint is available only from inside an Azure VM, at a fixed non-routable IP address. What is that IP address?

## Answers
^\s*169\.254\.169\.254\s*$

## Number of Retries
1
