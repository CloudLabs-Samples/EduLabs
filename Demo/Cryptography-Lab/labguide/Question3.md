## Metadata
Question Type : Text Input

## Question
3. In Lab 1 you ran `sha256sum -c invoice.sha256` to check both invoices against the vendor's published manifest. Which single word did that command print after the filename `invoice-resent.txt` to report that its hash did not match? Enter the word exactly as it appeared.

## Answers
^(?i)\s*(FAILED)\s*$

## Tags
Cryptography
Hashing
Integrity

## Number of Retries
1
