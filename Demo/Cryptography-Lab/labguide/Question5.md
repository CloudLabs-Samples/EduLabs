## Metadata
Question Type : Text Input

## Question
5. In Lab 1 you hashed the password `SamePassword123` twice with `-salt LabSalt01` and got identical output, then hashed the same password with `-salt LabSalt02` and got a completely different result. What is the one-word name for the value that changed between those two commands, and whose purpose is to ensure two users choosing the same password never share a stored hash?

## Answers
^(?i)\s*(the\s+)?(salt|salts)\s*$

## Tags
Cryptography
Hashing
Password Storage

## Number of Retries
1
