## Metadata
Question Type : Text Input

## Question
5. In Lab 3 you ran `ausearch -k identity` and saw a SYSCALL record containing both `uid=root` and a second identity field. That second field is fixed at login and cannot be altered afterwards, even by root, so it reveals which human account was behind an action performed via `sudo`. What is the name of that field?

## Answers
^(?i)\s*(auid)\s*$

## Tags
Linux
Auditing
Incident Response

## Number of Retries
1
